<h2><%= @user.username %>'s Photos</h2>
<% if is_logged_in? && logged_in_user.eql?(@user) %>
  <p><%= link_to 'Upload a new photo', new_user_photo_path %></p>
<% end %>
<%=will_paginate(@photos)%>
<ul id="photos">
  <%= render :partial =>@photos %>
</ul>
<%=will_paginate(@photos)%>